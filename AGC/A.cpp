#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, a, b, c, d, p=0;
    cin >> n >> a >> b >> c >> d;
    if (n   == 0) {
        b
    } else if (n%5 == 0) {
        n /= 5;
        p += c;
    } else if (n%3 == 0) {
        n /= 3;
        p += b;
    } else if (n%2 == 0) {
        n /= 2;
        p += a;
    } else {
        n = 
        p += d;
    }
    return 0;
}
